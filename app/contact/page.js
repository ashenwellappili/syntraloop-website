import ContactClient from './ContactClient';

export const metadata = {
  title: 'Contact SyntraLoop | Start a Project',
  description: 'Contact SyntraLoop about modern websites, business systems, AI integrations, and reliable digital solutions.',
};

export default function ContactPage() {
  return <ContactClient />;
}
